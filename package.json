{
  "name": "relic_logs",
  "version": "2.0.1",
  "description": "Request life cycle logging, generating atomic logs for each request",
  "main": "relic_logs.js",
  "dependencies": {
    "source-map-support": "^0.4.15"
  },
  "devDependencies": {
    "chai": "3.5.0",
    "eslint": "^3.19.0",
    "eslint-config-airbnb": "^14.1.0",
    "eslint-plugin-import": "^2.2.0",
    "eslint-plugin-jsx-a11y": "^4.0.0",
    "eslint-plugin-react": "^6.10.3",
    "express": "^4.13.4",
    "mocha": "3.0.1",
    "mock-require": "1.3.0",
    "sinon": "latest"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:madeinstefano/relic_logs.git"
  },
  "keywords": [
    "logs",
    "request",
    "node"
  ],
  "engines": {
    "node": "8.1.2",
    "npm": "3.10.6"
  },
  "scripts": {
    "lint:lib": "eslint lib/ --fix",
    "lint:root": "eslint rlc.js --fix && eslint relic_logs.js --fix && eslint x_relic_logs.js --fix",
    "lint": "npm run lint:lib && npm run lint:root",
    "test": "./node_modules/.bin/mocha --reporter spec --recursive ./spec/unit --timeout 2000"
  },
  "contributors": [
    {
      "name": "Stéfano Zanata",
      "email": "contato@szanata.com"
    }
  ]
}
